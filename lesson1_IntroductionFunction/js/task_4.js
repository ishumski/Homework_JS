// Напишите функцию sum которая складывает два числа
// Вызовите функцию и запишите ее возвращаемое значение в переменную sum

function getSumOfNumbers(a, b) {
    return a + b;
}
const sum = getSumOfNumbers(8, 23);
console.log(sum);